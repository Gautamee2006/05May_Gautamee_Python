Promt:
Create a complete and beginner-friendly Django REST Framework project for the following assignment.

==================================================
STEP 1: BUILD WITH AI - ORDER PLACEMENT API
==================================================

OBJECTIVE:

Build a Django REST Framework API that implements:

POST /api/orders/place/

The endpoint must use DRF APIView.

The API should accept customer_name, item, and quantity in the request body, validate the quantity, save the order to the database, and return the saved order details.

==================================================
PROJECT SETUP
==================================================

Create:

Project name:
food_delivery

App name:
orders

Use:

- Python
- Django
- Django REST Framework
- SQLite
- DRF APIView

Keep the project simple and beginner-friendly.

Do not create a frontend.

Do not use ModelViewSet for this task.

The main endpoint MUST be implemented using APIView.

==================================================
ORDER MODEL
==================================================

Create an Order model with these fields:

1. id
   - Auto-generated primary key

2. customer_name
   - CharField
   - max_length=100

3. item
   - CharField
   - max_length=100

4. quantity
   - IntegerField

The id must be automatically generated by Django.

Add a __str__ method.

==================================================
SERIALIZER
==================================================

Create OrderSerializer using ModelSerializer.

Fields:

- id
- customer_name
- item
- quantity

Add validation for quantity.

Quantity must:

- Be an integer
- Be greater than 0

If quantity is 0 or negative, raise a DRF ValidationError with a clear message:

"Quantity must be a positive integer."

Also handle invalid/non-integer quantity properly and return HTTP 400.

==================================================
API VIEW
==================================================

Create:

PlaceOrderAPIView

Use:

from rest_framework.views import APIView

Endpoint:

POST /api/orders/place/

The API should accept JSON request body like:

{
    "customer_name": "Gautamee",
    "item": "Pizza",
    "quantity": 2
}

==================================================
SUCCESS RESPONSE
==================================================

If the request is valid:

1. Validate the data.
2. Save the order to the database.
3. Return the saved order details.
4. Include the auto-generated id.
5. Return HTTP 201 Created.

Example successful response:

{
    "id": 1,
    "customer_name": "Gautamee",
    "item": "Pizza",
    "quantity": 2
}

HTTP Status:

201 Created

==================================================
VALIDATION FAILURE
==================================================

If quantity is invalid, for example:

{
    "customer_name": "Gautamee",
    "item": "Pizza",
    "quantity": 0
}

Return:

HTTP 400 Bad Request

Example:

{
    "quantity": [
        "Quantity must be a positive integer."
    ]
}

Also test:

quantity = -1

and invalid string values such as:

quantity = "abc"

These must not create an order in the database.

==================================================
URL CONFIGURATION
==================================================

Create the required URLs.

Final endpoint:

POST /api/orders/place/

Configure the main project urls.py to include the orders app URLs.

Example final URL:

http://127.0.0.1:8000/api/orders/place/

==================================================
DATABASE
==================================================

Use SQLite.

Run:

python manage.py makemigrations
python manage.py migrate

Make sure the Order table is created successfully.

==================================================
ADMIN PANEL
==================================================

Register Order in admin.py.

Create a superuser if needed:

python manage.py createsuperuser

Make sure orders can be viewed from Django Admin.

==================================================
POSTMAN TESTING
==================================================

The API must be tested end-to-end using Postman.

Provide clear instructions for testing.

TEST 1: SUCCESSFUL REQUEST

Method:

POST

URL:

http://127.0.0.1:8000/api/orders/place/

Headers:

Content-Type: application/json

Body → raw → JSON:

{
    "customer_name": "Gautamee",
    "item": "Pizza",
    "quantity": 2
}

Expected:

HTTP 201 Created

Response:

{
    "id": 1,
    "customer_name": "Gautamee",
    "item": "Pizza",
    "quantity": 2
}

==================================================
TEST 2: VALIDATION FAILURE
==================================================

Method:

POST

URL:

http://127.0.0.1:8000/api/orders/place/

Headers:

Content-Type: application/json

Body:

{
    "customer_name": "Gautamee",
    "item": "Pizza",
    "quantity": 0
}

Expected:

HTTP 400 Bad Request

Example response:

{
    "quantity": [
        "Quantity must be a positive integer."
    ]
}

Also test quantity = -1.

==================================================
POSTMAN SCREENSHOTS
==================================================

The assignment specifically requires screenshots.

Create a README.md section called:

"Postman Testing"

Mention that the following screenshots must be captured from the actual Postman application:

1. Successful POST request showing:
   - URL
   - Request body
   - HTTP 201 response
   - Saved order with auto-generated id

2. Validation failure POST request showing:
   - URL
   - quantity = 0
   - HTTP 400 response
   - Validation error message

Do NOT create fake screenshots.

If Postman cannot be opened automatically, provide exact instructions for the user to capture the screenshots manually.

Create a folder:

docs/screenshots/

and mention that screenshots can be saved there.

==================================================
README.md
==================================================

Create a simple README.md containing:

1. Project title
2. Objective
3. Technologies used
4. Project structure
5. Installation steps
6. Migration commands
7. Run server command
8. API endpoint
9. Request format
10. Successful response
11. Validation failure response
12. Postman testing steps
13. Screenshot instructions

==================================================
PROJECT STRUCTURE
==================================================

Create a clean structure similar to:

food_delivery/
│
├── manage.py
│
├── food_delivery/
│   ├── __init__.py
│   ├── settings.py
│   ├── urls.py
│   ├── asgi.py
│   └── wsgi.py
│
├── orders/
│   ├── __init__.py
│   ├── admin.py
│   ├── apps.py
│   ├── models.py
│   ├── serializers.py
│   ├── views.py
│   ├── urls.py
│   └── migrations/
│
├── docs/
│   └── screenshots/
│
└── README.md

==================================================
IMPORTANT REQUIREMENTS
==================================================

- Use DRF APIView.
- Use ModelSerializer.
- Save the order in SQLite database.
- Return auto-generated id.
- Quantity must be a positive integer.
- Invalid quantity must return HTTP 400.
- Successful order creation must return HTTP 201.
- Do not save invalid orders.
- Use proper HTTP status codes.
- Make sure all imports are correct.
- Make sure migrations work.
- Make sure the server runs without errors.
- Make sure the Postman endpoint works.
- Do not use unnecessary packages.
- Keep the code simple enough for a beginner to understand and explain in an interview.

==================================================
FINAL VERIFICATION
==================================================

Before finishing, verify:

[ ] Project runs successfully.
[ ] Database migrations work.
[ ] Order model works.
[ ] OrderSerializer works.
[ ] PlaceOrderAPIView uses APIView.
[ ] POST /api/orders/place/ works.
[ ] Valid quantity creates an order.
[ ] Auto-generated id is returned.
[ ] Successful response is HTTP 201.
[ ] quantity=0 returns HTTP 400.
[ ] quantity=-1 returns HTTP 400.
[ ] Invalid quantity does not create an order.
[ ] Order appears in database.
[ ] Order appears in Django Admin.
[ ] README.md is created.
[ ] Postman testing instructions are included.
[ ] Instructions for the two required screenshots are included.

After completing the project, show me the complete folder structure and the contents of models.py, serializers.py, views.py, urls.py, admin.py, settings.py changes, and README.md.

==================================================
Original AI Code:
==================================================
class OrderSerializer(serializers.ModelSerializer):
    class Meta:
        model = Order
        fields = ['id', 'customer_name', 'item', 'quantity']


    def validate_quantity(self, value):
        if value <= 0:
            raise serializers.ValidationError(
                "Quantity must be a positive integer."
            )
        return value

=========================================================
MYrrected Code
=========================================================
class OrderSerializer(serializers.ModelSerializer):
    class Meta:
        model = Order
        fields = ['id', 'customer_name', 'item', 'quantity']


    def validate_customer_name(self, value):
        if not value.strip():
            raise serializers.ValidationError(
                "Customer name cannot be empty."
            )
        return value


    def validate_quantity(self, value):
        if value <= 0:
            raise serializers.ValidationError(
                "Quantity must be a positive integer."
            )
        return value





I tested the AI-generated API using Postman and found that the customer_name field accepted an empty value. This could result in invalid order data being stored in the database. I added field-level validation to reject empty customer names. Now the API returns HTTP 400 for an empty customer name.